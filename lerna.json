{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "packages": ["apps/*", "libs/*"],
  "version": "independent",
  "npmClient": "pnpm",
  "command": {
    "version": {
      "ignoreChanges": ["*.md"],
      "message": "chore(release): tag & update changelog"
    }
  }
}
